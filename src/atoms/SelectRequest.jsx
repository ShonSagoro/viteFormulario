import "../assets/styles/selectrequest.css"

function SelecRequest(children) {
    return (  
        <>
            <select>
                {children}
            </select>
        </>
    );
}

export default SelecRequest;
